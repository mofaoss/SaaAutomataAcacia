SAA Python Environment Record

Date: 2026-03-11
Workspace: E:\workspace\SaaAssistantAca

1) Dependency model (pip-compile)
- requirements-prod.in  -> requirements-prod.txt
- requirements-dev.in   -> requirements-dev.txt
- requirements-release.in -> requirements-release.txt
- requirements.txt is kept as a compatibility alias:
  -r requirements-prod.txt

2) Environment layout (professional 3-env setup)
- saa-prod
  - purpose: production runtime/build
  - install: conda run -n saa-prod python -m pip install -r requirements-prod.txt
- saa-dev
  - purpose: daily development + tests
  - install: conda run -n saa-dev python -m pip install -r requirements-dev.txt
- saa-release
  - purpose: pre-release validation
  - install: conda run -n saa-release python -m pip install -r requirements-release.txt

3) Compile commands used
- conda run -n saa-release python -m piptools compile requirements-prod.in -o requirements-prod.txt
- conda run -n saa-release python -m piptools compile requirements-dev.in -o requirements-dev.txt
- conda run -n saa-release python -m piptools compile requirements-release.in -o requirements-release.txt

4) Notes
- dev/release inputs are constrained by requirements-prod.txt to keep lock versions aligned with production.
- old local env `saa` has been removed.
